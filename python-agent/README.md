# Solo Copilot Agent

FastAPI service for Sprint 6 Solo Copilot.

## Local Setup

```powershell
uv sync --frozen
```

During initial dependency changes, refresh the lockfile with:

```powershell
uv lock
```

Runtime configuration is provided through environment variables. See
`.env.example` once the deployment task lands.

